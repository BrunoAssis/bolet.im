<%= form_for(@teacher_class) do |f| %>
  <% if @teacher_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher_class.errors.count, "error") %> prohibited this teacher_class from being saved:</h2>

      <ul>
      <% @teacher_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :classroom_id %><br>
    <%= collection_select :teacher_class, :classroom_id, Classroom.all, :id, :to_s %>
  </div>

  <div class="field">
    <%= f.label :teacher_id %><br>
    <%= collection_select :teacher_class, :teacher_id, Teacher.all, :id, :to_s, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :subject_id %><br>
    <%= collection_select :teacher_class, :subject_id, Subject.all, :id, :to_s %>
  </div>
  
  <div class="field">
    <%= f.label :weekday %><br>
    <%= f.text_field :weekday %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.time_select :start_time, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.time_select :end_time, include_blank: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
